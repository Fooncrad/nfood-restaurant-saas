import { ArrowLeft, ArrowRight, Car, Check, ChevronLeft, Globe2, HardHat, KeyRound, Languages, Lock, Mail, MapPin, Phone, Scissors, ShieldCheck, ShoppingCart, Shirt, Store, Utensils, WalletCards } from "lucide-react";
import { useMemo, useState } from "react";
import { useLocation } from "wouter";
import { toast } from "sonner";
import { trpc } from "@/lib/trpc";
import { COUNTRIES, CURRENCIES, getCurrency } from "@shared/currencies";

const sectors = [{ id: "restaurant", label: "مطاعم ومأكولات", icon: Store }, { id: "fashion", label: "أزياء وموضة", icon: Shirt }, { id: "beauty", label: "صالونات وتجميل", icon: Scissors }, { id: "grocery", label: "بقالات وتموينات", icon: ShoppingCart }, { id: "auto", label: "خدمات سيارات", icon: Car }, { id: "maintenance", label: "صيانة وأشغال عامة", icon: HardHat }];
const languageOptions = [{ code: "ar", label: "العربية (RTL)" }, { code: "en", label: "English (LTR)" }, { code: "fr", label: "Français (LTR)" }];

export default function RegisterScreen() {
  const [, setLocation] = useLocation();
  const [step, setStep] = useState(1);
  const [done, setDone] = useState(false);
  const [sector, setSector] = useState("restaurant");
  const [languageCode, setLanguageCode] = useState("ar");
  const [form, setForm] = useState({ business: "", email: "", phone: "", city: "" });
  const [countryCode, setCountryCode] = useState("SA");
  const [currencyCode, setCurrencyCode] = useState("SAR");
  const [captchaAnswer, setCaptchaAnswer] = useState("");
  const registrationCaptcha = trpc.auth.registrationCaptcha.useQuery();
  const register = trpc.auth.registerRestaurant.useMutation({
    onSuccess: (result) => { setDone(true); toast.success("تم إنشاء حساب منشأتك. احفظ كلمة المرور المؤقتة وأكمل تأكيد البريد لاحقًا."); },
    onError: (error) => toast.error(error.message || "تعذر إنشاء الحساب")
  });
  const update = (key: keyof typeof form, value: string) => setForm((current) => ({ ...current, [key]: value }));
  const country = useMemo(() => COUNTRIES.find((item) => item.code === countryCode) ?? COUNTRIES[0], [countryCode]);
  const autoCurrency = getCurrency(country.currencyCode);
  const selectedCurrency = getCurrency(currencyCode);
  const languageLabel = languageOptions.find((item) => item.code === languageCode)?.label ?? languageOptions[0].label;
  const validContact = form.business.trim().length >= 2 && /^\S+@\S+\.\S+$/.test(form.email.trim()) && form.phone.trim().length >= 7 && form.city.trim().length >= 2;
  const next = () => { if (step === 2 && !validContact) { toast.error("أكمل البيانات المطلوبة بصيغة صحيحة"); return; } setStep((current) => Math.min(3, current + 1)); };
  const submit = () => { if (!registrationCaptcha.data?.challenge || !/^\d{1,2}$/.test(captchaAnswer.trim())) { toast.error("أكمل اختبار التحقق أولًا"); return; } register.mutate({ restaurantName: form.business.trim(), countryCode, currencyCode, primaryLanguage: languageCode as "ar" | "en" | "fr" | "ur" | "es" | "de" | "tr", country: country.nameAr, city: form.city.trim(), email: form.email.trim(), phone: form.phone.trim(), plan: "Free", captchaChallenge: registrationCaptcha.data.challenge, captchaAnswer: captchaAnswer.trim() }); };
  if (done && register.data) return (
    <div dir="rtl" className="min-h-screen bg-[#071525] text-slate-900"><div className="mx-auto grid min-h-screen max-w-[1500px] lg:grid-cols-[.75fr_1.25fr]"><aside className="relative hidden overflow-hidden bg-gradient-to-br from-orange-600 via-orange-500 to-amber-400 p-10 text-white lg:flex lg:flex-col lg:justify-between"><div className="absolute -left-24 -top-20 h-72 w-72 rounded-full bg-white/15 blur-3xl" /><div className="relative"><button onClick={() => setLocation("/")} className="flex items-center gap-3"><span className="flex h-11 w-11 items-center justify-center rounded-2xl bg-white text-xl font-black text-orange-600">N</span><span><strong className="block tracking-[.16em]">NFOOD</strong><small className="text-[9px] tracking-[.16em] text-orange-100">RESTAURANT OPERATING SYSTEM</small></span></button><div className="mt-24 max-w-sm"><p className="text-sm font-bold text-orange-100">بوابة المنشآت التجارية</p><h1 className="mt-4 text-5xl font-black leading-tight">ابدأ عملك<br />بشكل أذكى.</h1><p className="mt-6 text-sm leading-8 text-orange-50">أنشئ مساحة عمل مهيأة لنشاطك، واختر اللغة والعملة والمعلومات الأساسية قبل إطلاق عملياتك.</p></div></div></aside><main className="flex items-center bg-[#f8fafc] px-5 py-8 sm:px-10 lg:px-16"><div className="mx-auto w-full max-w-lg"><div className="mb-8 flex items-center justify-between"><button onClick={() => setLocation("/login")} className="flex items-center gap-2 text-sm font-bold text-slate-500 hover:text-orange-500"><ArrowRight className="h-4 w-4" /> العودة للدخول</button></div><div className="rounded-3xl border border-orange-100 bg-white p-8 shadow-sm"><div className="mx-auto flex h-16 w-16 items-center justify-center rounded-3xl bg-emerald-100 text-emerald-600"><Check className="h-8 w-8" /></div><p className="mt-6 text-center text-2xl font-black">تم إنشاء حساب منشأتك</p><p className="mx-auto mt-2 max-w-md text-center text-sm leading-7 text-slate-500">سجّل الدخول الآن بكلمة المرور المؤقتة التالية وسيُطلب منك تأكيد البريد الإلكتروني عند أول دخول.</p><div className="mx-auto mt-7 flex items-center justify-center gap-3 rounded-2xl border border-dashed border-orange-300 bg-orange-50 p-5"><KeyRound className="h-5 w-5 text-orange-600" /><code className="text-2xl font-black tracking-wider text-orange-900" dir="ltr">{register.data.temporaryPassword}</code></div><p className="mt-4 rounded-xl border border-sky-100 bg-sky-50 px-3 py-2 text-center text-[11px] leading-5 text-sky-800">رابط تأكيد البريد سُجّل لهذا الحساب (البريد: <span dir="ltr">{form.email}</span>) — أكمله لتأمين الحساب بالكامل.</p><div className="mt-7 grid gap-3"><button onClick={() => setLocation("/login")} className="h-12 rounded-2xl bg-[#e76f3c] text-base font-bold text-white hover:bg-[#d85f2e]">تسجيل الدخول الآن</button><button onClick={() => setLocation("/")} className="h-12 rounded-2xl border border-slate-200 text-sm font-bold text-slate-500 hover:border-orange-200">العودة للرئيسية</button></div></div></div></main></div></div>
  );
  return <div dir="rtl" className="min-h-screen bg-[#071525] text-slate-900"><div className="mx-auto grid min-h-screen max-w-[1500px] lg:grid-cols-[.75fr_1.25fr]"><aside className="relative hidden overflow-hidden bg-gradient-to-br from-orange-600 via-orange-500 to-amber-400 p-10 text-white lg:flex lg:flex-col lg:justify-between"><div className="absolute -left-24 -top-20 h-72 w-72 rounded-full bg-white/15 blur-3xl" /><div className="relative"><button onClick={() => setLocation("/")} className="flex items-center gap-3"><span className="flex h-11 w-11 items-center justify-center rounded-2xl bg-white text-xl font-black text-orange-600">N</span><span><strong className="block tracking-[.16em]">NFOOD</strong><small className="text-[9px] tracking-[.16em] text-orange-100">RESTAURANT OPERATING SYSTEM</small></span></button><div className="mt-24 max-w-sm"><p className="text-sm font-bold text-orange-100">بوابة المنشآت التجارية</p><h1 className="mt-4 text-5xl font-black leading-tight">ابدأ عملك<br />بشكل أذكى.</h1><p className="mt-6 text-sm leading-8 text-orange-50">أنشئ مساحة عمل مهيأة لنشاطك، واختر اللغة والعملة والمعلومات الأساسية قبل إطلاق عملياتك.</p></div></div><div className="relative space-y-3 text-sm text-orange-50"><p className="flex items-center gap-3"><Check className="h-4 w-4" /> إعداد سريع حسب قطاعك</p><p className="flex items-center gap-3"><Check className="h-4 w-4" /> كل دول العالم · عملة تُستنتج تلقائيًا</p><p className="flex items-center gap-3"><Check className="h-4 w-4" /> لغة الحساب تُحفظ من أول مرة</p></div></aside><main className="bg-[#f8fafc] px-5 py-8 sm:px-10 lg:px-16"><div className="mx-auto max-w-2xl"><div className="mb-8 flex items-center justify-between"><button onClick={() => setLocation("/login")} className="flex items-center gap-2 text-sm font-bold text-slate-500 hover:text-orange-500"><ArrowRight className="h-4 w-4" /> العودة للدخول</button><div className="flex items-center gap-2 lg:hidden"><span className="flex h-9 w-9 items-center justify-center rounded-xl bg-orange-500 font-black text-white">N</span><strong className="tracking-[.16em]">NFOOD</strong></div><button className="flex items-center gap-1 text-xs font-bold text-slate-500"><Globe2 className="h-4 w-4" /> {languageLabel}</button></div><div className="mb-8"><div className="mb-4 flex items-center gap-1 text-xs font-bold text-orange-600"><span className={`flex h-7 w-7 items-center justify-center rounded-full ${step >= 1 ? "bg-orange-500 text-white" : "bg-slate-200"}`}>1</span><span className="h-px w-10 bg-slate-200" /><span className={`flex h-7 w-7 items-center justify-center rounded-full ${step >= 2 ? "bg-orange-500 text-white" : "bg-slate-200 text-slate-400"}`}>2</span><span className="h-px w-10 bg-slate-200" /><span className={`flex h-7 w-7 items-center justify-center rounded-full ${step >= 3 ? "bg-orange-500 text-white" : "bg-slate-200 text-slate-400"}`}>3</span><span className="text-slate-400">3 خطوات</span></div><p className="text-sm font-bold text-orange-500">تأسيس مساحة جديدة</p><h2 className="mt-2 text-3xl font-black">أنشئ حساب منشأتك</h2><p className="mt-3 text-sm leading-7 text-slate-500">{step === 1 ? "ابدأ بتهيئة لغة التشغيل وقطاع نشاطك." : step === 2 ? "حدد الدولة والعملة ومعلومات التواصل — تُستنتج العملة تلقائيًا من الدولة ويمكن تغييرها." : "راجع ملخص منشأتك وأكمل اختبار التحقق الأمني لإنشاء الحساب."}</p></div>{step === 1 ? <section className="space-y-7"><div><div className="mb-3 flex items-center gap-2"><Languages className="h-4 w-4 text-orange-500" /><h3 className="font-black">لغة الحساب</h3></div><select value={languageCode} onChange={(event) => setLanguageCode(event.target.value)} className="h-12 w-full rounded-xl border border-slate-200 bg-white px-4 text-sm outline-none focus:border-orange-400"><option value="ar">العربية (RTL)</option><option value="en">English (LTR)</option><option value="fr">Français (LTR)</option></select><p className="mt-2 text-[11px] leading-5 text-slate-400">تُحفظ كلغة الحساب الافتراضية وتبدأ بها المنشأة، ويمكن تغييرها لاحقًا.</p></div><div><div className="mb-3 flex items-center justify-between"><h3 className="font-black">اختر قطاع نشاطك</h3><span className="text-xs text-slate-400">{sectors.length} قطاعات</span></div><div className="grid grid-cols-2 gap-3 sm:grid-cols-3">{sectors.map(({ id, label, icon: Icon }) => <button key={id} onClick={() => setSector(id)} className={`rounded-2xl border p-4 text-right transition ${sector === id ? "border-orange-400 bg-orange-50 ring-2 ring-orange-100" : "border-slate-200 bg-white hover:border-orange-200"}`}><span className={`flex h-10 w-10 items-center justify-center rounded-xl ${sector === id ? "bg-orange-500 text-white" : "bg-slate-100 text-slate-500"}`}><Icon className="h-5 w-5" /></span><span className="mt-3 block text-xs font-bold leading-5">{label}</span></button>)}</div></div><button onClick={next} className="flex h-12 w-full items-center justify-center gap-2 rounded-2xl bg-[#e76f3c] text-base font-bold text-white hover:bg-[#d85f2e]">متابعة <ArrowLeft className="h-5 w-5" /></button></section> : step === 2 ? <section className="space-y-5"><div className="rounded-2xl border border-orange-200 bg-orange-50 px-4 py-3 text-xs text-orange-800"><p className="font-black">قطاع النشاط</p><p className="mt-1 text-sm font-black">{sectors.find((item) => item.id === sector)?.label}</p></div><div className="grid gap-4 sm:grid-cols-2"><label className="block sm:col-span-2"><span className="mb-2 block text-xs font-bold text-slate-700">اسم المنشأة</span><input required value={form.business} onChange={(event) => update("business", event.target.value)} placeholder="مثال: شاورما السدة" className="h-12 w-full rounded-xl border border-slate-200 bg-white px-4 text-sm outline-none focus:border-orange-400 focus:ring-4 focus:ring-orange-100" /></label><label className="block sm:col-span-2"><span className="mb-2 flex items-center gap-2 text-xs font-bold text-slate-700"><MapPin className="h-4 w-4 text-orange-500" /> الدولة</span><select value={countryCode} onChange={(event) => { setCountryCode(event.target.value); setCurrencyCode(COUNTRIES.find((item) => item.code === event.target.value)?.currencyCode ?? "SAR"); }} className="h-12 w-full rounded-xl border border-slate-200 bg-white px-4 text-sm outline-none focus:border-orange-400">{COUNTRIES.map((item) => <option key={item.code} value={item.code}>{item.nameAr} · {item.name}</option>)}</select></label><label className="block"><span className="mb-2 flex items-center gap-2 text-xs font-bold text-slate-700"><WalletCards className="h-4 w-4 text-orange-500" /> العملة</span><div className="rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-2.5"><p className="text-[11px] font-bold text-emerald-700">تُستنتج تلقائيًا من الدولة</p><p className="mt-0.5 text-sm font-black text-emerald-900">{autoCurrency.nameAr} · {autoCurrency.code} ({autoCurrency.symbol})</p></div><select value={currencyCode} onChange={(event) => setCurrencyCode(event.target.value)} className="mt-2 h-12 w-full rounded-xl border border-slate-200 bg-white px-4 text-sm outline-none focus:border-orange-400">{CURRENCIES.map((item) => <option key={item.code} value={item.code}>{item.nameAr} · {item.code} ({item.symbol})</option>)}</select></label><label className="block"><span className="mb-2 flex items-center gap-2 text-xs font-bold text-slate-700"><MapPin className="h-4 w-4 text-orange-500" /> المدينة</span><input value={form.city} onChange={(event) => update("city", event.target.value)} placeholder="مثال: الرياض" className="h-12 w-full rounded-xl border border-slate-200 bg-white px-4 text-sm outline-none focus:border-orange-400 focus:ring-4 focus:ring-orange-100" /></label><label className="block"><span className="mb-2 flex items-center gap-2 text-xs font-bold text-slate-700"><Mail className="h-4 w-4 text-orange-500" /> البريد الإلكتروني</span><input type="email" dir="ltr" value={form.email} onChange={(event) => update("email", event.target.value)} placeholder="name@business.com" className="h-12 w-full rounded-xl border border-slate-200 bg-white px-4 text-sm outline-none focus:border-orange-400 focus:ring-4 focus:ring-orange-100" /></label><label className="block"><span className="mb-2 flex items-center gap-2 text-xs font-bold text-slate-700"><Phone className="h-4 w-4 text-orange-500" /> رقم الجوال</span><input type="tel" dir="ltr" value={form.phone} onChange={(event) => update("phone", event.target.value)} placeholder="+966 5X XXX XXXX" className="h-12 w-full rounded-xl border border-slate-200 bg-white px-4 text-sm outline-none focus:border-orange-400 focus:ring-4 focus:ring-orange-100" /></label></div><div className="flex items-center gap-3"><button type="button" onClick={() => setStep(1)} className="flex h-12 items-center justify-center gap-2 rounded-2xl border border-slate-200 bg-white px-5 text-sm font-bold text-slate-500 hover:border-orange-200"><ArrowRight className="h-4 w-4" /> السابق</button><button onClick={next} className="flex h-12 flex-1 items-center justify-center gap-2 rounded-2xl bg-[#e76f3c] text-base font-bold text-white hover:bg-[#d85f2e]">متابعة <ArrowLeft className="h-5 w-5" /></button></div></section> : <section className="space-y-5"><div className="rounded-2xl border border-slate-200 bg-white p-4 text-sm"><div className="flex items-center justify-between gap-3"><div><p className="text-xs text-orange-700">اسم المنشأة</p><p className="font-black">{form.business}</p></div><button type="button" onClick={() => setStep(2)} className="text-xs font-bold text-orange-600">تعديل</button></div><div className="mt-4 grid gap-3 sm:grid-cols-2"><p className="rounded-xl bg-slate-50 px-3 py-2 text-xs"><span className="block text-slate-400">دولة · عملة</span><span className="font-black text-slate-800">{country.nameAr} · {selectedCurrency.code}</span></p><p className="rounded-xl bg-slate-50 px-3 py-2 text-xs"><span className="block text-slate-400">لغة الحساب</span><span className="font-black text-slate-800">{languageOptions.find((item) => item.code === languageCode)?.label}</span></p><p className="rounded-xl bg-slate-50 px-3 py-2 text-xs"><span className="block text-slate-400">نداء الدخول</span><span className="font-black text-slate-800" dir="ltr">{form.email}</span></p><p className="rounded-xl bg-slate-50 px-3 py-2 text-xs"><span className="block text-slate-400">الباقة</span><span className="font-black text-slate-800">Free · فرع تشغيلي واحد</span></p></div></div><div className="rounded-2xl border border-orange-200 bg-orange-50 p-4"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-black text-orange-900">اختبار التحقق الأمني <span className="inline-flex items-center gap-1 text-orange-500"><ShieldCheck className="h-3.5 w-3.5" /></span></p><p className="mt-1 text-sm font-black text-orange-950">{registrationCaptcha.data?.prompt ?? "… + … = ؟"}</p></div><button type="button" onClick={() => { setCaptchaAnswer(""); void registrationCaptcha.refetch(); }} className="rounded-lg border border-orange-200 bg-white px-2.5 py-1.5 text-[10px] font-bold text-orange-800">سؤال جديد</button></div><input value={captchaAnswer} onChange={(event) => setCaptchaAnswer(event.target.value.replace(/\D/g, "").slice(0, 2))} inputMode="numeric" autoComplete="off" placeholder="الإجابة" aria-label="إجابة اختبار التحقق" className="mt-3 h-12 w-full rounded-xl border border-orange-200 bg-white px-4 text-sm outline-none focus:border-orange-400" /></div><div className="flex items-center gap-3"><button type="button" onClick={() => setStep(2)} disabled={register.isPending} className="flex h-12 items-center justify-center gap-2 rounded-2xl border border-slate-200 bg-white px-5 text-sm font-bold text-slate-500 hover:border-orange-200"><ArrowRight className="h-4 w-4" /> السابق</button><button type="button" onClick={submit} disabled={register.isPending} className="flex h-12 flex-1 items-center justify-center gap-2 rounded-2xl bg-[#e76f3c] text-base font-bold text-white hover:bg-[#d85f2e] disabled:opacity-60"><Lock className="h-4 w-4" /> {register.isPending ? "جارٍ إنشاء الحساب…" : "إنشاء الحساب"}</button></div></section>}</div></main></div></div>;
}